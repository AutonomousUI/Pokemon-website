# Pokemon Battle Simulator

Local Spring Boot app for Pokemon battles.

## Run Locally

```bash
./mvnw spring-boot:run
```

On Windows PowerShell:

```powershell
.\mvnw.cmd spring-boot:run
```

Then open:

```text
http://localhost:8080/
```

The app uses a local H2 database at `./data/battlesimulator`.
